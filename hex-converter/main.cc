#include <iostream>

/**
 * @brief 
 * 
 * @param org 
 * @param baseOrg 
 * @param baseDst 
 * @return std::string 
 */
std::string CvtAll(std::string org, int baseOrg, int baseDst)
{
    return "";
}

/**
 * @brief 
 * 1.十进制： 都是以0-9这九个数字组成，不能以0开头。
 * 2.二进制： 由0和1两个数字组成。
 * 3.八进制： 由0-7数字组成，为了区分与其他进制的数字区别，开头都是以0开始。
 * 4.十六进制：由0-9和A-F组成。为了区分于其他数字的区别，开头都是以0x开始。
 * 
 * @param argc 
 * @param argv 
 * @return int 
 */
int main(int argc, char* argv[])
{
    return 0;
}